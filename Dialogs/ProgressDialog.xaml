<Controls:MetroWindow x:Class="Zippy.Dialogs.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zippy.Dialogs"
        mc:Ignorable="d"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
     xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStyle="ToolWindow"
        Title="ProgressDialog" Height="218.394" Width="415.026" ResizeMode="NoResize" Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="79*"/>
            <RowDefinition Height="110*"/>
        </Grid.RowDefinitions>
        <ProgressBar x:Name="progress" Margin="0,87.078,0,0" Grid.Row="1"/>
        <TextBlock x:Name="Message" HorizontalAlignment="Center" TextWrapping="Wrap" Text="Extracting..." VerticalAlignment="Center" Grid.RowSpan="2"/>

    </Grid>
</Controls:MetroWindow>
